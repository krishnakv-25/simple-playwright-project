# Environment to run against: dev | staging | prod
ENV=dev

# Run browsers headless (true/false)
HEADLESS=true

# Slow down execution by N ms per action (useful for debugging/demos)
SLOW_MO=0

# Log verbosity: trace | debug | info | warn | error
LOG_LEVEL=info

# Set automatically by CI providers — do not set locally
# CI=true
